# My Portfolio

A lightweight portfolio landing page built with plain HTML, CSS, and JavaScript.

## Run locally

From the project root:

```bash
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
